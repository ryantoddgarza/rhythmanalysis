Math.mul = function multiply(x, y) {
  return x * y;
};

Math.div = function divide(x, y) {
  return x / y;
};

Math.add = function add(x, y) {
  return x + y;
};

Math.sub = function subtract(x, y) {
  return x - y;
};

module.exports = Math;
